pluginManagement {
    repositories {
        gradlePluginPortal()
        maven { url "https://repo.jorisg.com/snapshots" }
    }
}

rootProject.name = 'BrickStats'

include 'api'
include 'common'
include 'spigot'

